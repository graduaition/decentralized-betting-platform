const Solana = require('@solana/web3.js');

async function redistributeGains(winner) {
    // Fetch bets from db
    // Calculate gains
    // Redistributes funds
    // Save 1% fees to buy $graduation
}

module.exports = { redistributeGains };
